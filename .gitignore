# Dependencies
node_modules/

# Build outputs
dist/
.angular/
tmp/
out-tsc/

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
.idea/

# Env / secrets
.env
.env.local
.env.*.local
# NOTE: environment.ts files are committed with placeholder values so the app
# compiles on fresh clones. Replace the placeholders locally — do not commit
# real Supabase URLs / anon keys to public forks.

# Supabase
supabase/.branches/
supabase/.temp/
supabase/.env

# OS
.DS_Store
Thumbs.db
*.log
npm-debug.log*
